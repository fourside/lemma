# Lemma 使い方ガイド

## 事前準備（初回のみ）

### 1. Static コンテンツの生成

学習を始める前に、まず Phase 0 の教材を生成する。

```bash
cd lemma

# Phase 0（Course 0: 3回分）の全コンテンツを一括生成
./scripts/batch-generate.sh 0
```

これで以下が自動生成される:
- 音声用講義テキスト（3回分）
- テキスト講義資料（3回分）
- テスト問題プール（3回分）
- NotebookLM 音声ファイル（3回分）
- すべて R2 にアップロード済み

### 2. Podcast フィードの登録

Pocket Casts 等のアプリで以下のフィード URL を登録する:

```
https://<your-worker>.workers.dev/feed.xml
```

音声講義がエピソードとして配信される。

---

## 週次の学習サイクル

各 Week は 3 ステップで進める。

### Step 1: 音声講義を聴く（散歩中・30〜45分）

Podcast アプリまたは Lemma アプリ内の音声プレイヤーで再生する。

- 数式は出てこない。「なぜこのトピックが大事なのか」「直感的にどういう話か」を掴む回
- 聴き終えたらアプリで進捗を「音声済」に更新（Podcast アプリの既読でも管理可）

### Step 2: テキスト講義で深掘り（帰宅後・30〜45分）

Lemma アプリで該当 Week を開き、テキスト講義を読む。

- 数式・擬似コード・図を使った厳密な説明
- 例題が含まれているので、紙やエディタで一緒に解く
- 練習問題も付いている。解いてから模範解答を確認する
- 読み終えたら進捗を「テキスト済」に更新

**わからないことがあれば**、Claude チャット（このチャット）で質問する:

```
Lemma の Course 1 Week 3 を学習中です。
平衡二分探索木の回転操作がなぜ O(1) なのかがわかりません。
```

カリキュラムの該当範囲を踏まえて回答する。

### Step 3: テスト（別日・60〜90分）

Lemma アプリで該当 Week のテスト画面を開く。

- 問題プール（12問）からランダムに出題される
- 回答を入力して送信すると、Claude API が採点基準（rubric）に基づいて採点
- 各問題ごとにスコアとフィードバック（弱点指摘）が表示される

**結果に応じた次のアクション:**

| スコア | 判定 | 次にやること |
|--------|------|-------------|
| 80点以上 | ✅ 合格 | 次の Week へ進む |
| 60〜79点 | ⚠️ 補講 | 弱点にフォーカスした補講テキストが自動生成される。読んでから再テスト |
| 59点以下 | ❌ 再履修 | 同じ Week の音声→テキスト→テストをもう一度やる |

---

## Phase をまたぐとき

### 次の Phase の教材を先行生成する

現在の Phase の学習が半分ほど進んだら、次の Phase の教材を生成しておく。

```bash
# 例: Phase 1 の学習中に Phase 2 を先行生成
./scripts/batch-generate.sh 2
```

NotebookLM の非公式ツールが動かなくなるリスクがあるので、
早めにバッチ生成しておくのが安全。

### 生成済み教材を確認する

```bash
# R2 に何が入っているか確認
wrangler r2 object list lemma-lectures --prefix "audio/"
wrangler r2 object list lemma-lectures --prefix "lectures/"
wrangler r2 object list lemma-lectures --prefix "tests/"
```

---

## 教材の差し替え・調整

### 講義テキストが合わないと感じたら

特定の Week だけ再生成できる:

```bash
./scripts/generate-lecture.sh <course> <week>
```

### テスト問題を追加したい場合

Claude チャットで依頼する:

```
Lemma の Course 1 Week 2（探索とソート）の追加テスト問題を5問、
既存の問題プールと被らない形で生成してください。
JSON形式でお願いします。
```

出力された JSON をアプリから追加するか、R2 に直接アップロードする。

---

## 進捗の確認

Lemma アプリのダッシュボードで以下が確認できる:

- 現在の Week と全体の完了率
- 次にやるべきアクション（音声/テキスト/テスト）
- 過去のテストスコア一覧
- 弱点トピックの傾向（v2 で実装予定）

---

## Claude チャットでの依頼テンプレート

学習中に使える依頼の書き方:

**質問する:**
```
Lemma の Course X Week Y を学習中です。
〜がわかりません。
```

**講義テキストを再生成する:**
```
Lemma の Course X Week Y の音声用講義テキストを再生成してください。
前回のテスト結果で〜が弱かったので、そこを厚めにお願いします。
```

**テスト問題を追加生成する:**
```
Lemma の Course X Week Y の追加テスト問題を N 問生成してください。
```

**Phase 完了時の振り返り:**
```
Lemma の Phase X が完了しました。
テスト結果を踏まえて弱点を分析し、
Phase Y に入る前に補強すべきポイントを教えてください。
```
